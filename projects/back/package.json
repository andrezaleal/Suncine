{
  "name": "@sunne-test/back",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev:back": "tsx watch --env-file .env ./src",
    "lint": "biome check --fix --unsafe ./src/**",
    "lint:ci": "biome check --diagnostic-level=error ./src/**"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite-tsconfig-paths": "catalog:",
    "vitest": "catalog:"
  },
  "dependencies": {
    "fastify": "^5.1.0",
    "mongoose": "^8.8.1",
    "tsx": "^4.19.2"
  }
}
